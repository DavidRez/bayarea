header.navigation
  .navigation__container
    .navigation__col
      nuxt-link.navigation__links(
        v-for='(link, i) in links',
        :key='`link-${i}`',
        :class=`{ 'navigation__link--active': link.path === $route.path }`,
        :to='link.path',
        v-html='link.name'
      )
