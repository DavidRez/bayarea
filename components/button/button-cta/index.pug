.button-cta(:class='[ "button-cta--" + props.style, "button-cta--" + props.case ]')

  //- Nuxt Link ============================================
  nuxt-link.button-cta__button(
    v-if='props.type === `nuxt`',
    :to=`{ path: props.path, hash: props.hash }`,
    :tabindex='tabindex',
    :aria-label='props.aria',
  )
    .button-cta__container
      span(v-html='label')
    .button-cta__container
      BaseIcon(name='caret')

  //- Anchor Link ============================================
  a.button-cta__button(
    v-else-if='props.type === `anchor`',
    :href='props.href',
    :aria-label='props.aria',
    :tabindex='tabindex',
    :target='props.external ? `_blank` : null'
  )
    .button-cta__container
      span(v-html='label')
    .button-cta__container
      BaseIcon(name='caret')

  //- Custom Link ============================================
  button.button-cta__button(
    v-else-if='props.type === `custom`',
    @click='handleClick',
    :tabindex='tabindex',
    :aria-label='props.aria'
  )
    .button-cta__container
      span(v-html='label')
    .button-cta__container
      BaseIcon(name='caret')
